<Window x:Class="PharmacyClient.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PharmacyClient.ViewModels"
        xmlns:converters="clr-namespace:PharmacyClient.Converters"
        mc:Ignorable="d"
        Title="Вход в систему - Аптека" 
        Height="500" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
        </Style>
        
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <converters:EmptyToVisibilityConverter x:Key="EmptyToVisibilityConverter"/>
        <converters:BoolToYesNoConverter x:Key="BoolToYesNoConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="🏥 Аптечная Система" 
                       FontSize="24" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Выберите сотрудника для входа" 
                       FontSize="14"
                       Foreground="Gray"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Employee Selection -->
            <StackPanel Grid.Row="0">
                <TextBlock Text="Сотрудник:" FontWeight="Bold"/>
                <ComboBox ItemsSource="{Binding Employees}"
                          SelectedItem="{Binding SelectedEmployee}"
                          Height="30"
                          Margin="5">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding LastName, StringFormat='{}{0}'}" 
                                           FontWeight="Bold"/>
                                <TextBlock Text="{Binding FirstName, StringFormat='{}{0}'}" 
                                           FontSize="12" 
                                           Foreground="Gray"/>
                                <TextBlock Text="{Binding Position, StringFormat='Должность: {0}'}" 
                                           FontSize="11" 
                                           Foreground="DarkGray"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
                <Button Content="🔄 Обновить список" 
                        Command="{Binding LoadEmployeesCommand}"
                        Margin="5"/>
            </StackPanel>
            
            <!-- Selected Employee Info -->
            <Border Grid.Row="1" 
                    BorderBrush="#007ACC" 
                    BorderThickness="1" 
                    CornerRadius="5"
                    Padding="10"
                    Margin="5">
                <StackPanel Visibility="{Binding SelectedEmployee, Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Collapsed}">
                    <StackPanel.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,3"/>
                        </Style>
                    </StackPanel.Resources>
                    
                    <TextBlock Text="Информация о сотруднике:" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding SelectedEmployee.LastName, StringFormat='Фамилия: {0}'}"/>
                    <TextBlock Text="{Binding SelectedEmployee.FirstName, StringFormat='Имя: {0}'}"/>
                    <TextBlock Text="{Binding SelectedEmployee.Patronymic, StringFormat='Отчество: {0}'}"/>
                    <TextBlock Text="{Binding SelectedEmployee.Position, StringFormat='Должность: {0}'}"/>
                    <TextBlock Text="{Binding SelectedEmployee.Department, StringFormat='Отдел: {0}'}"/>
                    <TextBlock Text="{Binding SelectedEmployee.Phone, StringFormat='Телефон: {0}'}"/>
                    <TextBlock Text="{Binding SelectedEmployee.Email, StringFormat='Email: {0}'}"/>
                    
                    <Separator Margin="0,10"/>
                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Менеджер: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedEmployee.IsManager, Converter={StaticResource BoolToYesNoConverter}}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Подпись документов: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedEmployee.CanSignDocuments, Converter={StaticResource BoolToYesNoConverter}}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- Error Message -->
            <TextBlock Grid.Row="2" 
                       Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       TextWrapping="Wrap"
                       Margin="0,10,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource EmptyToVisibilityConverter}, FallbackValue=Collapsed}"/>
        </Grid>
        
        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Войти" 
                    Command="{Binding LoginCommand}"
                    Width="100"
                    IsEnabled="{Binding SelectedEmployee, Converter={StaticResource NullToBoolConverter}, FallbackValue=False}"/>
            <Button Content="Выход" 
                    Command="{Binding ExitCommand}"
                    Width="100"/>
        </StackPanel>
        
        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" 
                Background="#80FFFFFF" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                Child="{x:Null}"/>
    </Grid>
</Window>
