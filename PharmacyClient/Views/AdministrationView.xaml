<UserControl x:Class="PharmacyClient.Views.AdministrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и кнопки управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Администрирование пользователей" 
                       FontSize="20" 
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="0,0,20,0"/>
            
            <Button Content="Добавить" 
                    Command="{Binding AddUserCommand}"
                    Padding="15,8"
                    Margin="5,0"
                    Background="#4CAF50"
                    Foreground="White"/>
            
            <Button Content="Обновить" 
                    Command="{Binding RefreshCommand}"
                    Padding="15,8"
                    Margin="5,0"/>
        </StackPanel>

        <!-- Панель создания нового пользователя -->
        <Border Grid.Row="1" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,10"
                Background="#E3F2FD"
                Visibility="{Binding IsAddingUser, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" 
                           Text="Создание нового пользователя:" 
                           FontWeight="Bold"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Margin="0,0,20,5"/>

                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBox Width="200"
                             Margin="0,0,10,0"
                             VerticalAlignment="Center">
                        <TextBox.Text>
                            <Binding Path="NewLoginName" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <ExceptionValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    
                    <ComboBox ItemsSource="{Binding AvailableRoles}"
                              SelectedItem="{Binding SelectedRole}"
                              Width="150"
                              Margin="0,0,10,0"
                              VerticalAlignment="Center"/>
                    
                    <Button Content="Создать" 
                            Command="{Binding CreateUserCommand}"
                            Padding="15,8"
                            Background="#4CAF50"
                            Foreground="White"
                            VerticalAlignment="Center"/>
                    
                    <Button Content="Отмена" 
                            Command="{Binding CancelAddUserCommand}"
                            Padding="15,8"
                            Margin="5,0,0,0"
                            Background="#9E9E9E"
                            Foreground="White"
                            VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                           Text="• Введите логин нового пользователя (на латинице), выберите роль и нажмите 'Создать'. Пароль по умолчанию: 12345678" 
                           FontStyle="Italic"
                           Foreground="#666"
                           Margin="0,5,0,0"/>
            </Grid>
        </Border>

        <!-- Таблица пользователей -->
        <Border Grid.Row="2"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="5">
            <DataGrid ItemsSource="{Binding UserAccounts}"
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="False"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#F5F5F5"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      BeginningEdit="DataGrid_BeginningEdit"
                      CellEditEnding="DataGrid_CellEditEnding"
                      RowEditEnding="DataGrid_RowEditEnding">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Логин" Binding="{Binding LoginName, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                    <DataGridTextColumn Header="Роль" Binding="{Binding RoleName, UpdateSourceTrigger=PropertyChanged}" Width="130"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                    <DataGridTextColumn Header="Отчество" Binding="{Binding Patronymic, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                    <DataGridTextColumn Header="Должность" Binding="{Binding Position, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                    <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Индикатор загрузки -->
        <Border Grid.Row="2"
                Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10"/>
                <TextBlock Text="Загрузка данных..." 
                           HorizontalAlignment="Center"
                           FontSize="14"/>
            </StackPanel>
        </Border>

        <!-- Кнопки управления выбранным пользователем -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Удалить пользователя" 
                    Command="{Binding DeleteUserCommand}"
                    Padding="15,8"
                    Margin="0,0,10,0"
                    Background="#f44336"
                    Foreground="White"
                    IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBoolConverter}}"/>
            
            <Button Content="Сбросить пароль" 
                    Command="{Binding ResetPasswordCommand}"
                    Padding="15,8"
                    Margin="0,0,10,0"
                    Background="#FF9800"
                    Foreground="White"
                    IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBoolConverter}}"/>
            
            <TextBlock Text="{Binding StatusMessage}"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>