<UserControl x:Class="PharmacyClient.Views.AdministrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и кнопки управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Администрирование пользователей" 
                       FontSize="20" 
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="0,0,20,0"/>
            
            <Button Content="Создать пользователя" 
                    Command="{Binding OpenCreateUserScriptCommand}"
                    Padding="15,8"
                    Margin="5,0"
                    Background="#4CAF50"
                    Foreground="White"/>
            
            <Button Content="Удалить пользователя" 
                    Command="{Binding OpenDeleteUserScriptCommand}"
                    Padding="15,8"
                    Margin="5,0"
                    Background="#f44336"
                    Foreground="White"/>
            
            <Button Content="Сбросить пароль" 
                    Command="{Binding ResetPasswordCommand}"
                    Padding="15,8"
                    Margin="5,0"
                    Background="#FF9800"
                    Foreground="White"/>
            
            <Button Content="Обновить" 
                    Command="{Binding RefreshCommand}"
                    Padding="15,8"
                    Margin="5,0"/>
        </StackPanel>

        <!-- Панель информации -->
        <Border Grid.Row="1" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,10"
                Background="#FFF3E0">
            <StackPanel>
                <TextBlock Text="Инструкция по управлению пользователями:" 
                           FontWeight="Bold"
                           FontSize="14"
                           Margin="0,0,0,5"/>
                <TextBlock Text="• Для создания нового пользователя: добавьте сотрудника через вкладку 'Сотрудники', затем выполните скрипт create_employee_login.sql в SSMS" 
                           TextWrapping="Wrap"
                           Margin="0,2"/>
                <TextBlock Text="• Для удаления пользователя: выполните скрипт delete_employee_login.sql в SSMS, затем удалите сотрудника" 
                           TextWrapping="Wrap"
                           Margin="0,2"/>
                <TextBlock Text="• Для сброса пароля: выберите пользователя в списке и нажмите 'Сбросить пароль'" 
                           TextWrapping="Wrap"
                           Margin="0,2"/>
                <TextBlock Text="• Пароль по умолчанию для всех пользователей: 12345678" 
                           FontStyle="Italic"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Таблица пользователей -->
        <Border Grid.Row="2"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="5">
            <DataGrid ItemsSource="{Binding UserAccounts}"
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#F5F5F5"
                      HeadersVisibility="Column"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Логин" Binding="{Binding LoginName}" Width="120"/>
                    <DataGridTextColumn Header="Роль" Binding="{Binding RoleName}" Width="130"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="120"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="100"/>
                    <DataGridTextColumn Header="Должность" Binding="{Binding Position}" Width="150"/>
                    <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Индикатор загрузки -->
        <Border Grid.Row="2"
                Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10"/>
                <TextBlock Text="Загрузка данных..." 
                           HorizontalAlignment="Center"
                           FontSize="14"/>
            </StackPanel>
        </Border>

        <!-- Статус и информация -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       FontWeight="Bold"/>
            <TextBlock Text=" | " Margin="10,0"/>
            <TextBlock Text="Все изменения в учетных записях SQL Server выполняются через скрипты в SSMS."
                       FontStyle="Italic"
                       Foreground="#666"/>
        </StackPanel>
    </Grid>
</UserControl>
